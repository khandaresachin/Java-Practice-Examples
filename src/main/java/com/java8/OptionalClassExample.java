package com.java8;

public class OptionalClassExample {
    /*
        This method is used for bla blaaa...........
     */
    public static void main(String args[]){

        String[] words =new String[10];
        String alpha = words[7].toUpperCase();

        System.out.println(alpha);

        /*
            Student student = new Student();
            student=10
            Student std = db.getStudentById(); // data present and no data
            if(std.getMarks() > 75)
                "Congrats ! you rocked it !!"

            using Optional code

              Optional<Student> std = db.getStudentById();
                if(std.isPresent()){
                      if(std.get().getMarks() > 75)
                "Congrats ! you rocked it !!"
                }
                std.isEmpty()


         */
    }
}
